import { SectionContainer } from './SectionForm'

const EmptySection = ({ label }: { label: string }) => {
  return <SectionContainer>Create New section for {label}</SectionContainer>
}

export default EmptySection
